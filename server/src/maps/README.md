# Maps

Contiene le `BoardConfig` di default (es. `classic.json`), il layout
standard usato dalla modalità Classic. Popolata a partire dalla Fase 1
(vedi `docs/ROADMAP.md`) — la forma dei dati è definita in
`shared/src/index.ts` (`BoardConfig`, `Tile`).
