# URL del server Socket.IO a cui il client si connette.
# In locale di default punta a http://localhost:3001 (vedi lib/socket.ts),
# non serve impostarla per sviluppare.
# In produzione: l'URL pubblico del server (Railway/Render/Fly.io).
VITE_SERVER_URL=https://morichup-server.up.railway.app
